//  this is the file where i test different transformation orders


// translate along plane, rotate around model center, zoom into screen center. this seemed the most sensible to me.

//translate(width/2, height/2, -size);
//scale(scale);
//translate(tx, ty);
//rotateX(rx);
//rotateY(ry);

// translate along plane, rotate around upper left corner, zoom into model.

//scale(scale);
//translate(tx, ty);
//rotateX(rx);
//rotateY(ry);
//translate(width/2, height/2, -size);

// translate along plane, rotate around model center, zoom into upper left corner.

//scale(scale);
//translate(tx, ty);
//translate(width/2, height/2, -size);
//rotateX(rx);
//rotateY(ry);

// NEEDED: translate along plane, rotate around screen center, zoom into screen center.
